# Production: point at hosted API (include https scheme, no trailing slash)
VITE_API_URL=https://your-api.onrender.com

# Leave blank for local dev — Vite proxy forwards /api to http://localhost:5000 automatically.
# VITE_API_URL=
